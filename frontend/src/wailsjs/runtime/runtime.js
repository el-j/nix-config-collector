// Wails runtime stub — replaced by the real runtime when built with Wails.
export function EventsOn(eventName, callback) {}
export function EventsEmit(eventName, ...data) {}
export function WindowSetTitle(title) {}
